# 批次 95–104 修复清单

> 创建时间: 2026-07-24
> 基线版本: v1.3.50
> 目标版本: v1.3.51
> 批次范围: 95–104

---

## 问题总览

| # | 问题 | 根因 | 关联批次 |
|---|------|------|----------|
| 1 | 基础数据菜单无排序，字典管理在最下方 | `basic-data.html` MODULES 硬编码顺序 | 100 |
| 2 | 权限管理页 `page_company` / `mod_basic` 出现两条 | `seedMenusFromRegistry` 并发调用 → 重复插入 | 96, 97 |
| 3 | 权限管理页中文显示不全 | `menuName` 为空时回退到 code | 97, 98 |
| 4 | 权限管理页各层级都需显示中文 | 同 #3 | 98 |
| 5 | 角色管理页显示 `?` 号 | 源码存在 U+FFFD 污染 | 95 |
| 6 | 角色新增时权限列表显示 `?` 或空 | `n.name` → 应为 `n.menuName` | 95, 99 |
| 7 | 管理员角色未自动新增 | `ensureDefaultAdminRole` 仅在登录页调用 | 101 |
| 8 | 「安装到主屏」「检查更新」文案显示 `?` | 源码 U+FFFD 污染（4 文件 146 处） | 95 |

---

## 执行清单

### 批次 95 — U+FFFD 字符清理

| 文件 | U+FFFD 数 | 策略 |
|------|-----------|------|
| `role.js` | 49 | 按上下文推断还原 |
| `login/classic.html` | 49 | 同上 |
| `about.html` | 45 | 同上 |
| `app.js` | 3 | 同上（line 3045） |
| **合计** | **146** | 产出 `BATCH95_RESTORED.md` 供核对 |

### 批次 96 — seedMenusFromRegistry 单例门控

**文件**: `permissions.js`

- 新增 `_seedPromise` 全局单例
- `seedMenusFromRegistry` 先检查 `_seedPromise`，存在则直接返回
- 循环内：`existing` 命中时用 `updateMenu` 补齐 `menuName`（upsert）
- 失败时释放 `_seedPromise` 允许重试

### 批次 97 — buildMenuTree 去重

**文件**: `permissions.js`

- `buildMenuTree` 追加去重逻辑：同 `menuCode` 的记录合并 children，只保留第一个为父节点

### 批次 98 — 权限管理页中文显示

**文件**: `permission.js`

- `buildTreeHtml`：`menuName` 为空时回退到 `RT_PERM_REGISTRY_API.getRegistryEntry(code).name`
- label 格式：「中文名 · code」

### 批次 99 — 角色管理页权限树中文

**文件**: `role.js`

- `buildTreeHtml` L135：`n.name` → `n.menuName`
- 空值时回退到注册表 `getRegistryEntry(code).name`

### 批次 100 — 基础数据菜单排序

**文件**: `basic-data.html`

- 每项 MODULES 加 `sort` 字段
- `render()` 按 `sort` 升序排列

最终顺序：

| sort | 模块 |
|------|------|
| 10 | 公司管理 |
| 20 | 部门管理 |
| 30 | 职位管理 |
| 40 | 人员管理 |
| 50 | 项目管理 |
| 60 | 项目版本管理 |
| 70 | 角色管理 |
| 80 | 权限管理 |
| 90 | 字典管理 |

### 批次 101 — role.html / permission.html 入口 admin seed

**文件**: `role.html`、`permission.html`

- `init()` 中串联：
  ```js
  RT_USERS.ensureDefaultAdminRole({ account: 'admin', password: '123', nickname: '管理员', operator: 'system' })
    .then(() => RT_PERMISSIONS.seedMenusFromRegistry('system'))
  ```

作用：
- 角色管理页入口 → 自动创建「系统管理员」角色 + 绑定 admin 账号
- 权限管理页入口 → 保障菜单注册表播种

### 批次 102 — 编码规范：UTF-8 无 BOM（强制）

**文件**: `RULES.md`、`.githooks/pre-commit`

- RULES.md 新增 §6 文件编码规则：
  - 所有源文件（`.html`/`.js`/`.css`/`.md`/`.json`）必须 UTF-8 无 BOM
  - 禁止 `U+FFFD` 替换字符
- 新增 `pre-commit` hook：扫描暂存文件，检测到 `\xef\xbf\xbd` 拒绝提交
- 安装方式：`ln -sf ../../.githooks/pre-commit .git/hooks/pre-commit`

### 批次 103 — 回归测试

**文件**: `tests/test-batch95-utf8.js`、`tests/test-batch96-seed-dedup.js`、`tests/test-batch101-admin-role.js`

### 批次 104 — 文档与发版 ✅ 已完成

**文件**: `CHANGELOG.md`、`version.json`

- ✅ 运行 `release.sh 1.3.51` 统一升级版本号（7 处版本号 + ?v= 全站漂移自检）
- ✅ 生成 `CHANGELOG.md` v1.3.51 条目，汇总 95–104 全部工作
- ✅ 版本号: `1.3.50` → `1.3.51`

---

## 95–104 工作总结

### 编码修复（批次 95）
- 全站 4 文件 146 处 U+FFFD 替换字符清零：`role.js`(49)、`login/classic.html`(49)、`about.html`(45)、`app.js`(3)
- `login/classic.html` 和 `about.html` 从 Git 历史提取干净基线 → 重新应用 RBAC 增量修补
- 产出 `BATCH95_RESTORED.md` 逐处核对清单

### RBAC 稳定性加固（批次 96–99）
- **批次 96**: `seedMenusFromRegistry` 加 `_seedPromise` 单例门控，防止并发调用导致重复播种
- **批次 97**: `buildMenuTree` 追加同 `menuCode` 去重逻辑，合并 children + 回填空 `menuName`
- **批次 98**: 权限管理页 `buildTreeHtml` — `menuName` 为空时回退注册表 `getRegistryEntry()`，label 格式为「中文名 · code」
- **批次 99**: 角色管理页权限树 — `n.name` → `n.menuName` + 注册表兜底，中文显示修复

### 菜单排序与入口增强（批次 100–101）
- **批次 100**: 基础数据页 `MODULES` 增加 `sort` 字段 (10–90)，`render()` 按排序输出；字典管理固定末尾
- **批次 101**: `role.html` + `permission.html` 入口串联 `ensureDefaultAdminRole()` → `seedMenusFromRegistry()`，保障管理员角色和权限菜单自动播种

### 编码规范与测试（批次 102–103）
- **批次 102**: `RULES.md` §6 UTF-8 无 BOM 编码规则 + `.githooks/pre-commit` 拦截 U+FFFD
- **批次 103**: 新增 3 个测试文件 13 条回归测试 (batch95 utf8 扫描 + batch96 seed 幂等 + batch101 admin 角色自创)

### 发版（批次 104）
- 版本 `1.3.50` → `1.3.51`，`release.sh` 全站 ?v= 漂移自检通过
- `CHANGELOG.md` 写入 v1.3.51 条目汇总 95–104 全部改动

---

## 决策记录

| 决策 | 内容 |
|------|------|
| D1 | 方案 B：按上下文推断还原 U+FFFD，产出核对清单 |
| D2 | 字典管理排最后（sort=90），顺序：公司→部门→职位→人员→项目→项目版本→角色→权限→字典 |
| D3 | admin seed 仅放 `role.html` + `permission.html`，不放所有入口 |
| D4 | 新要求：添加编码规范批次 102，规定所有源文件 UTF-8 无 BOM + pre-commit hook 拦截 |
